// Stub do módulo `server-only` do Next para ambiente de teste (vitest/jsdom).
// O original lança erro ao ser importado fora do servidor; nos testes de
// integração precisamos importar Server Actions/queries server-side.
export {};
